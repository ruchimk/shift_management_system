<!-- main section for the form -->
<div class="container tall">
    <div class="signup_container">
    <%= form_for(resource, as: :user, url: create_user_path, html: {class: "form01"}) do |f| %>
    <%= devise_error_messages! %>
    <h2 class="label">Sign Up your Company</h2>
        <span class="label">Company Name:</span>
        <%= text_field_tag :company_name, nil, class: "g_inpt" %>
        <span class="label">Email</span>
        <%= f.email_field :email, class: "g_inpt"  %>
        <span class="label">First Name:</span>
        <%= f.text_field :first_name, class: "g_inpt" %>
        <span class="label">Last Name:</span>
        <%= f.text_field :last_name, class: "g_inpt"  %>
        <span class="label">Profile Picture:</span>
        <%= f.label :avatar , class: "g_inpt" %><br />
        <%= f.file_field :avatar %>
        <span class="label">Password:</span>
        <%= f.password_field :password, class: "g_inpt" %>
        <span class="label">Password Confirmation:</span>
        <div class="clear"></div>
        <%= f.password_field :password_confirmation, class: "g_inpt" %>
        <%= f.hidden_field(:is_admin, value: true) %>
        <%= f.submit "Sign up", class: "sub_btn" %>
        <%= link_to "Cancel", root_path, class: "cancel" %>
    <% end %>
    </div>
</div>

